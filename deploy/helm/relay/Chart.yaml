apiVersion: v2
name: relay
description: Relay — a lightweight, self-hosted API Gateway written in Go.
type: application
# Chart version. Bump on chart changes.
version: 0.1.0
# Default application version deployed by the chart (the container image tag).
# Overridable via image.tag.
appVersion: "0.1.0"
home: https://github.com/algoryn-io/relay
sources:
  - https://github.com/algoryn-io/relay
keywords:
  - api-gateway
  - reverse-proxy
  - gateway
maintainers:
  - name: algoryn-io
