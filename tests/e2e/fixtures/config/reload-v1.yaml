routes:
  - name: reload
    match:
      path_prefix: /reload
      methods: [GET]
    backend: reload

backends:
  - name: reload
    strategy: round_robin
    instances:
      - url: http://upstream-v1:8080
